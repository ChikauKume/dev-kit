# 統合テストケース定義
# PHPUnit Feature Tests (Infrastructure/Presentation層のテスト)

# 現在、明示的な統合テストケースは未定義
# 以下は将来的に追加予定:

# repositories:
#   - id: REPO-001
#     name: EloquentUserRepository
#     tests: [...]

# controllers:
#   - id: CTRL-001
#     name: AuthController
#     tests: [...]

# Note: 現在の実装では、統合テストは手動で作成されています
# tests/Feature/Modules/User/ 配下を参照
