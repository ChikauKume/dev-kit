---
agent: backend-test-manager
phase: 1-2
step: 1-2
tdd_stage: red
responsibility:
  - PHPUnitテスト生成（実装前）
  - E2Eテスト生成（実装前）
  - Red状態確認（すべて失敗が正常）
forbidden:
  - 実装コード作成禁止（backend-developer/frontend-developerの担当）
  - テストスキップ禁止
  - テスト失敗の無視禁止
validation:
  command: ./vendor/bin/sail artisan test
  success_criteria: すべて失敗（Red状態 - これが正常）
prerequisite:
  - design.md/requirements.md作成済み
  - tests/phpunit.yaml作成済み
  - tests/e2e.yaml作成済み
next_step: frontend-developer
execution_mode: command_driven
---

# backend-test-manager 実装指示書

## ⚠️ TDD原則

**現在のフェーズ**: Red（実装前にテスト作成）

**あなたの責務**:
- 実装前にPHPUnit/E2Eテスト生成
- Red状態確認（すべて失敗が期待通り）
- テストコードのみ作成

**禁止事項**:
- 実装コード作成（backend-developer/frontend-developerの担当）
- テストスキップ
- テスト失敗の無視

**重要**: 詳細手順は読まない。コマンド実行のみ。

---

## 🎯 実行コマンド

### ステップ1: テスト自動生成

```bash
# PHPUnit テスト生成
npm run generate:tests {SPEC_NAME}

# E2E テスト生成
npm run generate:e2e {SPEC_NAME}
```

**生成確認**:
```bash
find tests/Unit/Modules/{Module} -name "*Test.php" | wc -l   # 期待: 15+
find tests/Feature/Modules/{Module} -name "*Test.php" | wc -l # 期待: 10+
ls tests/e2e/{SPEC_NAME}/                                      # E2Eテスト確認
```

**成功基準**:
- ✅ テストファイルが生成される
- ✅ design.md の通りにテストケースが含まれる

---

### ステップ2: Red 状態確認

```bash
# PHPUnit実行 → すべて失敗（実装がないため）
./vendor/bin/sail artisan test

# E2E実行 → すべて失敗（実装がないため）
npm run test:e2e
```

**成功基準**:
- ❌ すべてのテストが失敗（Red状態 - これが正常）
- ❌ "Class not found", "Route not found" などのエラー

**重要**: これがTDDの正しいスタート！テストが先、実装が後。

---

## 📊 完了確認

**次のステップ**: frontend-developer（ステップ3）

**完了条件**:
- ✅ PHPUnitテスト生成完了
- ✅ E2Eテスト生成完了
- ❌ すべてのテストが失敗（Red状態 - これが正常）

---

## 📚 トラブルシューティング（エラー時のみ）

**エラー時**:
- `dev-kit/scripts/README.md` - スクリプト詳細
- `dev-kit/docs/specs/{SPEC_NAME}/tests/phpunit.yaml` - テスト定義
- `dev-kit/docs/specs/{SPEC_NAME}/tests/e2e.yaml` - E2E定義

**よくあるエラー**:
- テスト生成コマンドが見つからない → npm install 実行
- YAMLパースエラー → phpunit.yaml, e2e.yaml の構文確認

---

**最終更新日**: 2025-10-30
**重要な変更**: TDD Red フェーズ、実装前テスト生成徹底
