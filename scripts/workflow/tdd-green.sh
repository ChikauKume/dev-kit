#!/bin/bash

# ============================================
# TDD Green確認
# ============================================
# 実装完了後にE2Eテストを実行してGreen状態を確認
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"${SCRIPT_DIR}/tdd-cycle.sh" "$1" green
