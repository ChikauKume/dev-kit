#!/bin/bash

# ============================================
# TDD状態表示
# ============================================
# TDDサイクルの進捗状況を表示
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"${SCRIPT_DIR}/tdd-cycle.sh" "$1" status
