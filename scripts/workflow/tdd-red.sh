#!/bin/bash

# ============================================
# TDD Red確認
# ============================================
# 次の画面のE2Eテストを実行してRed状態を確認
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"${SCRIPT_DIR}/tdd-cycle.sh" "$1" red
